let RoutePaths = {
  home: "/",
  about: "/about",
  //   route: (id) => (id ? `/product/${id}` : "/product/:id"),
};

// let path = [
//   {
//     route: (id) => (id ? `/product/${id}` : "/product/:id"),
//     component: <Home />,
//   },
// ];

export default RoutePaths;
